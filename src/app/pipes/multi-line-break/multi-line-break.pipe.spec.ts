import { MultiLineBreakPipe } from './multi-line-break.pipe';

describe('MultiLineBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiLineBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
